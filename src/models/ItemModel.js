export default class ItemModel {
  constructor(brand, title, description, descriptionFull, currency) {
    this.brand = brand;
    this.title = title;
    this.description = description;
    this.descriptionFull = descriptionFull;
    this.currency = currency;
  }
}
